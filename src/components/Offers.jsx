import { useEffect } from "react";
import { useState } from "react";
import { useDispatch, useSelector } from "react-redux";
import { getNewsLoadingStatus, loadNews } from "../store/reducers/newsSlice";

const Offers = () => {
    return (
        <section>
            <p>Offers</p>
        </section>
    );
};

export default Offers;
